{
  "name": "cryo-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1-schema": "wrangler d1 execute cryo-db --local --file=./migrations/0001_initial_schema.sql",
    "types": "wrangler types",
    "test": "vitest",
    "migrate-data": "node scripts/migrate-data.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240919.0",
    "@types/node": "^22.4.1",
    "prettier": "^3.3.3",
    "typescript": "^5.4.5",
    "vitest": "^1.6.0",
    "wrangler": "^4.77.0"
  },
  "dependencies": {
    "hono": "^4.4.5"
  }
}
